/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
   images: {
    unoptimized: true,
    } // This line is already correct, no need to change
};

module.exports = nextConfig;


module.exports = nextConfig;